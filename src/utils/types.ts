
export type resumeProps = {
  showLinks: boolean,
  screenWidth: number,
}

export type navBarProps = {
  showLinks: boolean,
  setShowLinks: Function
}

export type windowSize = {
  width: number,
  height: number,
}